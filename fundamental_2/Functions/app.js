'use strict';
/*
// const interface = "alireza";

function sayMyCompleteName(firstName, lastName) {
	const myName = `firstName: ${firstName}, lastName: ${lastName}`;
	return myName;
}

sayMyCompleteName('alireza', 'fazeli');
console.log(sayMyCompleteName('alireza', 'fazeli'));

function fruitMixer(fruitOne, fruitTwo) {
	const mixer = `${fruitOne} mixed with ${fruitTwo} and now you eat it.`;
	return mixer;
}

const newFruitMixer = fruitMixer('apple', 'orange');

console.log(newFruitMixer);

// python print function
function print(anyThing) {
	console.log(anyThing);
}

print('Hello World');

// ***** function expression *****

const sayHello = function () {
	console.log('hello world');
};

sayHello();

// function expression if used const variable cant never changes.
// sayHello = function () {
//     console.log("hellow world");
// }

const helloToMe = function (hello) {
	if (hello == 'hello') {
		console.log('hello to you my friend 😄');
	} else if (hello === undefined) {
		console.log('hey are you there? 😖');
	} else {
		console.log('you should say hello to me 😱');
	}
};

helloToMe();

// **********************************
// arrow function
const nameMixing = (firstName, lastName) => {
    const names = `your name is ${firstName} and your last name is ${lastName} `;
    return names
};

console.log(nameMixing("alireza", "fazeli"));

const sayHello = () => console.log("Hello world");

sayHello();


function fruitCuter (fruitCount) {
    return fruitCount * 4
}

function fruitMixer (fruitOne, fruitTwo, fruitOneName, fruitTwoName) {
    const fruitOneCount = fruitCuter(fruitOne);
    const fruitTwoCount = fruitCuter(fruitTwo);

    const mixerMachine = `(${fruitOneCount} ${fruitOneName} peaces) Mixed with (${fruitTwoCount} ${fruitTwoName}peaces) . Mixxxxxeeee ..... ⚙⚙`;
    return mixerMachine
}

console.log(fruitMixer(5, 2, "apple", "orange"));

*/