// declare variable

// variable
let userName = "Alireza Fazeli";
console.log(userName);

// date in variable can change
userName = "Mohsen Ghasemi";
console.log(userName);


// constants 
// if you sure your data not change use const
const userFamily = "Fazeli";
console.log(userFamily);

// data cant change in const
userFamily = "ghasemi";
console.log(userFamily);
