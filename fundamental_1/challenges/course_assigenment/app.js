/*
// equality operators
const nameNeighbor = Number(prompt(
	'How many neighbour countries does your country have ?'
));

if (nameNeighbor === 1) {
	console.log('Only 1 border!');
} else if (nameNeighbor > 1) {
	console.log('More than 1 border');
} else {
	console.log('No borders');
}

*/

/*
// logical operators
// language should be ENG
// count should less than 50 million
// country shouldn't have island
const countryName = 'iran';
const countryCount = 50;
const countryLanguage = 'Fa';
const countryIsIsland = false;

if (
	countryLanguage == 'ENG' &&
	countryIsIsland == false &&
	countryCount <= 50
) {
	console.log(`You should live in ${countryName} :)`);
} else {
	console.log(`${countryName} does not meet your criteria :(`);
}

*/

/*
// the switch statement
switch ('hindi') {
	case 'chinese':
	case 'mandarin':
		console.log('MOST number of native speakers!');
		break;

	case 'spanish':
		console.log('2nd place in number of native speakers');
		break;

	case 'english':
		console.log('3rd place');
		break;

	case 'hindi':
		console.log('Number 4');
		break;

	case 'arabic':
		console.log('5th most spoken language');
		break;

	default:
		console.log('Great language too :D');
}

*/

/*
// The Conditional (Ternary) Operator
const countryName = "iran";
const countryPopulation = 33;

countryPopulation >= 33 ? console.log(`${countryName} population is above average`) : console.log(`${countryName} population is below average`);
*/