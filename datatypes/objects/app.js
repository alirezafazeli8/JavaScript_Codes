const users = {
    userName: "alireza",
    city: "Tehran",
    age: 16,
};

console.log(users);

let showUserInfo = `
    welcome to database. XD

    username : ${users.userName}
    city : ${users.city}
    age : ${users.age}

    Your Information So Cute
`;

console.log(showUserInfo);