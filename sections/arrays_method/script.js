"use strict";

// const moneys = [100, -33, 34, -2123, -4232, 44234];

// const { raft, amad } = moneys.reduce(
//   function (l, f) {
//     l[f > 0 ? "raft" : "amad"] += f;
//     return l;
//   },
//   { raft: 0, amad: 0 }
// );

// console.log(raft, amad);

// convert title case function

// const convertTitle = function (title) {
//   // unknown array
//   const unknownWord = ["and", "a", "or", "then"];
//   // title word array
//   let word = title.trim().split(" ");

//   //   return word.filter(function (cWord) {
//   //     unknownWord.forEach(function (unWord) {
//   //       return cWord != unWord;
//   //     });
//   //   });
// };

// console.log(convertTitle("hello my name is alireza and i wanna a or then now"));

// const randomInt = function (min, max) {
//   return Math.floor(Math.random() * (max - min + 1) + min);
// };

// console.log(randomInt(3, 5));
