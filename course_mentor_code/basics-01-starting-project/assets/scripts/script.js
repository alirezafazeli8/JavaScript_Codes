// make function for (persian: baraye jam kardan XD).
function sumNumber (num1, num2) {
    const sum = num1 + num2;
    return sum
}

// declaring variable for showing a operators in real
let x;
let y;

// thats my calculators result
outputResult(sumNumber(x = 47203749024, y = 743274027492 ), `${x} + ${y}`);

